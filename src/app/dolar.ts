import { CompilerFacadeImpl } from '@angular/compiler/src/jit_compiler_facade';

export interface Dolar {
    nombre: string;
    compra: number;
    venta: number;
    porcentajeVariacion24horas: any;
}
